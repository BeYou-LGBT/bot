export const Settings = {
	guild: process.env.GUILD_ID,
	channels: {
		log: process.env.LOG_CHANNEL,
		suggestion: process.env.SUGGESTION_CHANNEL,
	},
	suggestion: {
		maxSuggestionThreadName: 80,
	},
};
